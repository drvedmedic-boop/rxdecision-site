# RX Decision website

Static launch site for RX Decision.

## Local preview

```bash
python -m http.server 8080
```

Then open `http://localhost:8080`.

## Deployment

Designed for GitHub Pages with custom domain `rxdecision.com`.
The repository includes `CNAME`, `robots.txt`, and `sitemap.xml`.
